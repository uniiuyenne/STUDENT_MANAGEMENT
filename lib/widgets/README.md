# Widgets
This folder contains reusable UI components.

## Structure
- `common/` - Common reusable widgets used across the app
- `filters/` - Filter-related widgets and components

## Guidelines
- Widgets should be small, focused, and reusable
- Pass data via constructor parameters
- Use providers for state management when needed
