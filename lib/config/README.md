# Configuration
This folder contains application configuration files.

## Files
- `firebase_options.dart` - Firebase configuration (generated by FlutterFire CLI)
- `supabase_config.dart` - Supabase configuration and initialization

## Setup
1. Run `flutterfire configure` to generate Firebase options
2. Update `supabase_config.dart` with your Supabase project credentials
