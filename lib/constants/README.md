# Constants
This folder contains application-wide constants and configuration values.

## Files
- `app_constants.dart` - App-wide constants (URLs, timeouts, magic numbers, etc.)

## Guidelines
- Keep all magic numbers, strings, and URLs here
- Avoid hardcoding values in implementation files
